/**
 * Settings Route
 * 
 * Full-screen settings page accessible from the Profile tab header.
 * Includes account info, preferences, privacy settings, and account management.
 */

import React from 'react';
import { SettingsScreen } from '@/src/components/settings';

export default function SettingsRoute() {
  return <SettingsScreen />;
}

