/**
 * Home Tab Route
 * 
 * This file exists for Expo Router routing but the actual content
 * is rendered by the parent _layout.tsx which manages all tab content
 * inside a unified ContentSheet overlay.
 */
export default function HomeRoute() {
  // Content is rendered by parent layout
  return null;
}
